# Q-OPSEC KMS legacy runtime dependencies
# Standard-library modules (ctypes, importlib, platform, pprint, ssl,
# subprocess, tempfile, urllib and warnings) are intentionally omitted.
SQLAlchemy
cryptography
fastapi
# Optional PQC backend; the service has a classical fallback.
oqs
pydantic
redis
uvicorn[standard]
