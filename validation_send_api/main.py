from prometheus_fastapi_instrumentator import Instrumentator
cat: /mnt/c/Projetos/Q-OPSEC/validation_send_api/main.py: No such file or directory

Instrumentator().instrument(app).expose(app, endpoint='/metrics')
