# Q-OPSEC KMS Service V2 runtime dependencies
# Standard-library modules (ctypes, dataclasses, importlib, platform, pprint,
# ssl, subprocess, tempfile, urllib and warnings) are intentionally omitted.
cryptography
fastapi
numpy
# Optional PQC backend; the application degrades gracefully when unavailable.
oqs
psutil
pydantic
uvicorn[standard]
